apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: unrelated-egress
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/name: api
  policyTypes:
    - Egress
  egress:
    - to:
        - ipBlock:
            cidr: 10.0.0.0/8
      ports:
        - port: 8080
          protocol: TCP
