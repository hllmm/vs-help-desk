# Optional build-time API origin (no trailing slash).
# Leave empty for same-origin relative /api/... (Vite dev proxy or production reverse proxy).
# Example absolute override (bypasses Vite proxy): VITE_API_BASE_URL=http://127.0.0.1:5154
VITE_API_BASE_URL=
