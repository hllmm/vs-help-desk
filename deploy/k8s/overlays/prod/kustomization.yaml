apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../base
# The base local image references are intentionally left unresolved here.
# scripts/render-prod-manifest.sh must substitute operator-supplied verified
# digest references before this manifest can be deployed.
patches:
  - path: ingress-patch.yaml
